[package]
name = "elf_loader"
version = "0.1.0"
authors = ["sclolus"]
edition = "2018"

[dependencies]
elf_parser = { path = "../elf_parser"}
terminal = { path = "../terminal" }
errno = { path = "../errno" }
itertools = "0.8.0"

[features]
test = []
std-print = []